import takor from '../..'
    
describe('takor.pojo', () => {
    it('runs without throwing', () => {
        // const asserter = takor.pojo(takor.any)
        // expect(() => asserter(10)).not.toThrow()
    })
})
