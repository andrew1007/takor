import takor from '../..'
    
describe('takor.falsey', () => {
    it('runs without throwing', () => {
        // const asserter = takor.falsey(takor.any)
        // expect(() => asserter(10)).not.toThrow()
    })
})
